عنوان المهمة: إصلاح نظام الإشعارات في مشروع “y2025”

الهدف: تمكين نظام الإشعارات ليعمل بشكل موثوق على جانب الخادم (server) والعميل (client) في تطبيق y2025، بحيث يستقبل المستخدم إشعارات فورية (real-time) أو مجدولة حسب التصميم، وتُعرض بالشكل المحدد في دليل التصميم (design_guidelines.md).

نطاق العمل:

مراجعة كود جانب الخادم (folder server) المسؤول عن توليد وإرسال الإشعارات.

مراجعة كود جانب العميل (folder client) لاستقبال وعرض الإشعارات، بما في ذلك التكامل مع واجهات المستخدم وCSS/TAILWIND الخاصة بها.

التحقق من بنية shared (folder shared) إذا كانت هناك نماذج (models) أو أنواع (types/interfaces) تستخدم في إشعارات، وضمان تناسقها بين العميل والخادم.

اختبار سيناريوهات الإشعارات: على سبيل المثال، إشعار عند حدث معين، إشعار مجدول، إشعار في الخلفية، إشعار عند فتح التطبيق/في الخلفية.

إصلاح أي خطأ يُمنع الإشعارات من العمل: (مثال: مشكلة في الربط بقاعدة البيانات، أو مشكلة WebSocket/Push، أو مشكلة في الأذونات أو المكونات الطرفية).

اقتراح وتحسين واجهة الاستخدام لتجربة المستخدم (UX) والإشعارات: تضمين توقيتات مناسبة، عرض واضح، وتنبيه غير مُعدد أو مزعج.

توصيات إضافية:

راجع ملف design_guidelines.md في المستودع ليتأكد من أن عرض الإشعارات يتماشى مع نظام الألوان والخطوط والتوجيهات. 
GitHub

تأكد من أنّ عملية الإرسال من الخادم تشمل تسجيل أي فشل في الإشعار (logging) لتسهيل التصحيح لاحقاً.

إذا لم تكن هناك بنية Push Notifications (مثل Firebase Cloud Messaging أو Web Push) فعلياً، أقترح اعتمادها أو دمجها كمكون جديد.

أضف اختباراً تلقائياً (unit/integration) يغطي سيناريو الإشعار للتأكد من أنّه مستقر قبل التسليم.

عدّل README أو أضف وثيقة جديدة (مثلاً NOTIFICATIONS.md) تُوضح كيف تعمل بنية الإشعارات، كيفية إضافتها، وكيفية اختبارها.

نتيجة متوقعة: عند الانتهاء، يجب أن يكون النظام قادراً على إرسال إشعارات تلقائية، وأن يستلمها المستخدمون في العميل، ويُعرضونها بطريقة واضحة وجذّابة، مع وجود سجل أخطاء (log) لأي فشل محتمل.

🔍 ما لاحظته ويستحق الانتباه

المستودع مكتوب بـ TypeScript (≈ 96%) وCSS/TAILWIND. 
GitHub

هناك دليل تصميم (design_guidelines.md) — مما يدل على وجود توجيهات واجهة مستخدم واضحة. هذا يُسهل تعديل واجهة الإشعارات بما يتماشى معها.

هيكل المجلدات: client, server, shared — يعني أن هناك تقسيم بين جانب العميل والخادم، ما يستدعي التأكد من التزامن بينهما (مثلاً: نفس نوع الـNotification أو API endpoints).

لا يوجد ما يشير مباشرة إلى مكتبة إشعارات/Push مثبتة في النظرة السريعة (لم تتح لي بعد فحص التفاصيل الداخلية) — لذا قد يكون النظام يحتاج تكامل إضافي.

بما أنك عملت على التطبيق مع Riplet.ai، فالأفضل أن تتأكد أنك توفر له صلاحية الوصول إلى المستودع أو نسخة branch testing لتفادي التأثير على نسخة الإنتاج.